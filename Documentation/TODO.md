# Web Harness TODO

## Quick Wins
- Add `version` command to the web harness.
- Add `get metadata` command to the web harness.
- Keep `test-results summary` exposed and stable.
- Add `test-results tests` command to the web harness.

## Low Effort
- Add `test-results metrics` (optional testId) to the web harness.
- Add `test-results insights` to the web harness.
- Add `format description` to the web harness.
- Add `graph` to the web harness.

## Medium Effort
- Add `test-results test-details` (requires testId picker) to the web harness.
- Add `test-results activities` (requires testId picker) to the web harness.
- Add `get object` (id input + format options) to the web harness.
- Add `compare` (two bundles + diff output) to the web harness.

## Higher Effort
- Add `export diagnostics/attachments/metrics/object` (download zip) to the web harness.
- Add `merge` (multi-upload + output bundle/zip) to the web harness.
- Add `format description diff` (two bundles + diff view) to the web harness.
- Add attachment/log viewer UI (preview + filters) to the web harness.
