import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Insight Sample")
                .font(.title)
            Text("Generate xcresults")
                .font(.subheadline)
        }
        .padding()
    }
}
